name: Create Release
run-name: New release from tag ${{ github.ref_name }} by @${{ github.actor }}

on:
  push:
    tags:
      - '[0-9]+\.[0-9]+\.[0-9]+'

jobs:
  call-shared-release::
    uses: WritePoetry/create-release-composite-actionl@v1
    with:
      artifacts_path: 'build'